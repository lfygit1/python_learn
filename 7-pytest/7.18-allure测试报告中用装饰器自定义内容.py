"""
llure测试报告中用装饰器自定义内容

allure 装饰器介绍
@allure.epic   # 用来定义一个场景
@allure.feature   # 用来定义一个功能
@allure.story   # 用来定义一个故事
@allure.title   # 用来定义一个标题
@allure.testcase   # 用来定义一个用例的链接地址
@allure.issue   # 缺陷  对应缺陷管理系统里面的链接
@allure.desscription  # 用例的描述
@allure.step   # 用来定义一个步骤
@allure.severity   # 用来定义一个用例等级
@allure.link   # 用来定义一个链接
@allure.attach   # 用来定义一个附件


allure 装饰器
step、feature、story、issue、description、severity、link、attach 的
使用
用例参考 D:\python_learn\7-pytest\test_demo_allure 中的 test_demo_allure02.py

"""